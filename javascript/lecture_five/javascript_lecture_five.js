/* const yearOfFly = 1961;
let count = 0; //счетчик итераций
for (let i = 2022; yearOfFly < i; i-=1) { //цикл начинается с 2022; пока yearOfFly меньше i, условие правдиво, цикл выполняется; i уменьшается на 1 с каждой итерацией (шагом)
    count +=1;
} 
console.log(count) */
// const yearOfFly = 1961;
/* let count = 0;
for (let i = 2022; 1800 < i; i -= 1) {
    if (i === 1961) {
    break; //остановка цикла
  }
  count += 1;
}
console.log(count); */
/* function showMessage (message) {
    console.log(message);
}
showMessage("Hi")
showMessage("Hello")
showMessage("Привет")
showMessage("Как дела") */
/* function sum(a, b) {
  return a + b
}
const result = sum(3, 5)
console.log(result) */
let a = 1;
function mathematicalAddition(b) {
  let a = 2;
  return a+b;
}
console.log(mathematicalAddition(3));

/* const names = ["Alex", "Ivan", "Tony", "Olga"]
const findElem = names.find((a) => a === "Tony");
console.log(findElem) */
/* const arr = [1, -2, 5, -6, 9, -8, 5]
const result = arr.reduce((prev, cur) => {
    if (cur > 0) {
        return prev + cur;
    }
    return prev
});
console.log(result) */
